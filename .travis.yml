matrix:
  include:
    - language: python
      python:
        - "3.7"
      before_install:
        - cd python
        - python --version
        - pip install -U pip
        - pip install -U pytest
      install:
        - pip install -r requirements.txt
      before_script:
        - cd python
      script: pytest